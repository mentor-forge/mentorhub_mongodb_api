_locked: false
file_name: Card.0.0.0.yaml
root:
  additional_properties: false
  description: Discovery dashboard polymorphic card payloads (configurator-only schema;
    not a Mongo collection). Root one_of of Customer, Profile, and Notification variants
    with constant type discriminators.
  name: Card
  properties:
  - description: ID of source document
    name: _id
    required: false
    type: identifier
  - description: The Card Type (Collection Aligned)
    enums: card_type
    name: type
    required: false
    type: enum
  - description: The card title
    name: name
    required: false
    type: sentence
  - description: The card contents
    name: description
    required: false
    type: markdown
  - description: The url the card links to
    name: link
    required: false
    type: url
  required: false
  type: object
