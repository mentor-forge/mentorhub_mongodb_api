_locked: false
file_name: email.yaml
root:
  description: A valid email address (user@domain)
  name: email
  required: false
  schema:
    pattern: ^[^\s@]+@[^\s@]+\.[^\s@]+$
    type: string
  type: simple
